import { AppCard, PageHeader } from '@/shared/ui/components';

export default function UnauthorizedPage() {
  return <AppCard><PageHeader title="Unauthorized" description="Please sign in with an account that can access this STMS area." /></AppCard>;
}
