import { IntegratedResourcePage } from '@/shared/components/integrated-resource-page';
export default function Page({ params }: { params: { id: string } }) { return <IntegratedResourcePage title={`Ticket ${params.id}`} description="Ticket detail from existing Ticket API." endpoint={`/v1/tickets/${params.id}`} queryKey={`ticket-${params.id}`} currentRole="customer" allowedRoles={['customer','admin','owner']} realtimeTopic="ticket events" columns={[{key:'id',label:'Ticket'},{key:'status',label:'Status'},{key:'booking_id',label:'Booking'},{key:'created_at',label:'Created'}]} />; }
